function [ output_args ] = Daub_CompositionStep( v, n )
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here

    v3 = 3.0;
    sqrt3 = sqrt(3.0);
    
    half = bitshift(n,1); 
    denom = 4.0 * sqrt(2.0);
    result = zeros(1,length(n));
    
    a = *(v + half - 1); 
    b = *(v + n - 1); 
    c = *v; 
    d = *(v + half); 

 
    t1 = c - a; 
t2 = b - d; 



end

