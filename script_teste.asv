%img = imread('mario.jpg');
%s = im2double('mario.jpg');
%s = double(s);
%s = s(1:1:4);
n = 4;
s = zeros(n);

for i=1:size(s,1)
    juca = double(s);
    juca(0) = 9.0;
    juca(1) = 7.0;
    juca(2) = 15.0;
    juca(3
    [result] = Daub_CompositionStep(juca, n);
end;